# Hermes Documentation

The long-term Hermes language-layer documentation system now lives in:

`/Users/cc/Obsidian Vault/10_Projects/Hermes_Agent`

This repository keeps runtime code, plugin logic, ops tooling, tests, and local state. Do not recreate generated language-layer planning docs here unless a future task explicitly asks for a temporary working artifact.

Runtime prompt material such as `SOUL.md`, credentials, config files, sessions, logs, DBs, cache, PID/lock files, plugins, ops code, tests, and `lang-layer/config.json` are not documentation cleanup targets.
